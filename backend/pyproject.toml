[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "B"]
ignore = [
    "B008",  # FastAPI's Query()/Depends() in argument defaults is the idiomatic pattern
]
